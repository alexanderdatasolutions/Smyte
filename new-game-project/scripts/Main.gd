# scripts/Main.gd
# Main game entry point - GameCoordinator is now autoload
extends Node

func _ready():
	print("Main: Game loaded with clean architecture")
	print("Main: GameCoordinator autoload will handle initialization")
